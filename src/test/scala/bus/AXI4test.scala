package bus

import chisel3._
import chisel3.util._
import chisel3.iotesters._

// class AXI4Tester(c: AXI4Bundle) extends PeekPokeTester(c) {
  // define which csrfile, 341 makes up MEPC, we do not care others
  // because others will be decoded before the step
  
// }

// class AXI4Test extends ChiselFlatSpec {
//   "AXI4Test" should "pass" in {
//     iotesters.Driver.execute(Array(), () => new AXI4Bundle) {
//       c => new AXI4Tester(c)
//     }
//   }
// }
